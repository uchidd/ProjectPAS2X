<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.uchidd.projectpas2xaplikasionlineshopelektronik">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_pas_2"
        android:label="LocRide Store"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BeliProdukActivity"></activity>
        <activity android:name=".DetailProdukActivity" />
        <activity android:name=".RecylerViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>